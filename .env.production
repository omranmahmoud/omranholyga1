# Production environment variables (do not commit secrets beyond what is needed for frontend)
# Backend base URL
VITE_API_BASE=https://omraneva.onrender.com
VITE_API_URL=https://omraneva.onrender.com
EXPO_PUBLIC_API_URL=https://omraneva.onrender.com

# Tablet policy (adjust as needed)
MOBILE_WEB_TABLET_POLICY=desktop

# Optional external mobile site (if Netlify mobile subdomain used instead of /m)
# MOBILE_WEB_EXTERNAL_URL=https://m.yourdomain.com

# Only include PUBLIC vars here. Private keys (Mongo, JWT, Cloudinary secret) stay in hosting provider config, not this file.
